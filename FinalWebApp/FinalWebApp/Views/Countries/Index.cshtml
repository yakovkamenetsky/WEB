@model IEnumerable<FinalWebApp.Models.Country>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create" class="btn btn-info">+</a>
</p>

<div class="col-centered">
	<form action="/countries/SearchCities" id="All-cities"></form>
	<input placeholder="Search all cities in country" name="countryName" type="text" form="All-cities"/>
	<button class="btn btn-info" type="submit" form="All-cities" hidden>Search all cities in the country</button>
</div>

<table class="table table-info table-striped">
    <thead class="thead-light">
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
